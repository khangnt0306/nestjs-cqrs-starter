import { ICommand } from '@nestjs/cqrs';
import { Update{{pascalCase name}}Dto } from '@shared/dtos/{{camelCase name}}s';

export class Update{{pascalCase name}}Command implements ICommand {
  constructor(
    public readonly {{camelCase name}}Id: string,
    public readonly dto: Update{{pascalCase name}}Dto,
  ) {}
}

