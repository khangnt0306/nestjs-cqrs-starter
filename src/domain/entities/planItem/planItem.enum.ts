export enum PlanItemType {
  INCOME = 'INCOME',
  EXPENSE = 'EXPENSE',
}

export enum DistributeType {
  EVENLY = 'EVENLY',
  CUSTOM = 'CUSTOM',
}

export enum EXCLUDE_TYPE {
  FIXED = 'FIXED',
  FLEXIBLE = 'FLEXIBLE',
}
